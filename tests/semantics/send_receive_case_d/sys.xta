// Place global declarations here.
int fid_var150_main = 78;

int func79_count = 0;
int cap_cid_var153_chA[1];
chan async_func79[1];
chan sync_func79[1];
int make_func79() {
    int pid = func79_count;
    func79_count++;
    return pid;
}

int func80_count = 0;
int cap_cid_var154_chA[1];
chan async_func80[1];
chan sync_func80[1];
int make_func80() {
    int pid = func80_count;
    func80_count++;
    return pid;
}

int chan_count = 0;
int chan_counter[10];
int chan_buffer[10];
chan sender_alpha[10];
chan sender_omega[10];
chan receiver_alpha[10];
chan receiver_omega[10];
chan close[10];

int make_chan(int buffer) {
    int cid = chan_count;
    chan_count++;
    chan_counter[cid] = 0;
    chan_buffer[cid] = buffer;
    return cid;
}


process func80(int[0, 0] pid) {
// Place local declarations here.
bool is_sync;
int p = -1;
int cid_var154_chA = -1;
void initialize() {
    cid_var154_chA = cap_cid_var154_chA[pid];
}

state
    starting,
    ending,
    enter_loop_cond0,
    exit_loop_cond0,
    exit_loop0,
    started,
    ended,
    enter_loop_body0,
    exit_loop_body0;
init
    starting;
trans
    enter_loop_cond0 -> exit_loop_cond0 { },
    enter_loop_body0 -> exit_loop_body0 { },
    started -> enter_loop_cond0 { },
    exit_loop_cond0 -> enter_loop_body0 { },
    exit_loop_cond0 -> exit_loop0 { },
    exit_loop_body0 -> enter_loop_cond0 { },
    exit_loop0 -> ending { },
    starting -> started { sync async_func80[pid]?; assign is_sync = false, initialize(); },
    starting -> started { sync sync_func80[pid]?; assign is_sync = true, initialize(); },
    ending -> ended { guard is_sync == false; },
    ending -> ended { guard is_sync == true; sync sync_func80[pid]!; };
}

process Channel(int[0, 9] i) {
// Place local declarations here.

state
    confirming_closed,
    bad,
    new_sender,
    confirming_a,
    confirming_b,
    closing,
    closed,
    idle,
    new_receiver,
    confirming_closing;
commit
    confirming_a,
    confirming_b,
    confirming_closed,
    new_sender,
    new_receiver,
    confirming_closing,
    closing;
init
    idle;
trans
    idle -> new_sender { sync sender_alpha[i]?; assign chan_counter[i]++; },
    new_sender -> idle { guard chan_counter[i] > chan_buffer[i]; },
    new_sender -> confirming_a { guard chan_counter[i] <= chan_buffer[i]; sync sender_omega[i]!; },
    confirming_a -> idle { guard chan_counter[i] > 0; },
    confirming_a -> idle { guard chan_counter[i] <= 0; sync receiver_omega[i]!; },
    idle -> new_receiver { sync receiver_alpha[i]?; assign chan_counter[i]--; },
    new_receiver -> idle { guard chan_counter[i] < 0; },
    new_receiver -> confirming_b { guard chan_counter[i] >= 0; sync receiver_omega[i]!; },
    confirming_b -> idle { guard chan_counter[i] < chan_buffer[i]; },
    confirming_b -> idle { guard chan_counter[i] >= chan_buffer[i]; sync sender_omega[i]!; },
    idle -> confirming_closing { guard chan_counter[i] < 0; sync close[i]?; },
    confirming_closing -> confirming_closing { guard chan_counter[i] < 0; sync receiver_omega[i]!; assign chan_counter[i]++; },
    confirming_closing -> closing { guard chan_counter[i] == 0; },
    idle -> closing { guard 0 <= chan_counter[i] && chan_counter[i] <= chan_buffer[i]; sync close[i]?; },
    idle -> bad { guard chan_counter[i] > chan_buffer[i]; sync close[i]?; },
    closing -> closed { assign chan_counter[i] = 1,
chan_buffer[i] = -1; },
    closed -> confirming_closed { sync receiver_alpha[i]?; },
    confirming_closed -> closed { sync receiver_omega[i]!; },
    closed -> bad { sync sender_alpha[i]?; },
    closed -> bad { sync close[i]?; };
}

process main() {
// Place local declarations here.
bool is_sync;
int p = -1;
int cid_var151_chA = -1;
int cid_var152_chB = -1;

state
    made_cid_var152_chB_0,
    started_func80_0,
    starting,
    started,
    made_cid_var151_chA_0,
    created_func79_0,
    started_func79_0,
    created_func80_0,
    ending,
    ended,
    closed_cid_var152_chB_0;
init
    starting;
trans
    started -> made_cid_var151_chA_0 { assign cid_var151_chA = make_chan(0); },
    made_cid_var151_chA_0 -> made_cid_var152_chB_0 { assign cid_var152_chB = make_chan(0); },
    made_cid_var152_chB_0 -> closed_cid_var152_chB_0 { sync close[cid_var152_chB]!; },
    closed_cid_var152_chB_0 -> created_func79_0 { assign p = make_func79(), cap_cid_var153_chA[p] = cid_var151_chA; },
    created_func79_0 -> started_func79_0 { sync async_func79[p]!; },
    started_func79_0 -> created_func80_0 { assign p = make_func80(), cap_cid_var154_chA[p] = cid_var151_chA; },
    created_func80_0 -> started_func80_0 { sync async_func80[p]!; },
    started_func80_0 -> ending { },
    starting -> started { },
    ending -> ended { };
}

process func79(int[0, 0] pid) {
// Place local declarations here.
bool is_sync;
int p = -1;
int cid_var153_chA = -1;
void initialize() {
    cid_var153_chA = cap_cid_var153_chA[pid];
}

state
    sent_cid_var153_chA_0,
    starting,
    started,
    ending,
    ended,
    sending_cid_var153_chA_0;
init
    starting;
trans
    started -> sending_cid_var153_chA_0 { sync sender_alpha[cid_var153_chA]!; },
    sending_cid_var153_chA_0 -> sent_cid_var153_chA_0 { sync sender_omega[cid_var153_chA]?; },
    sent_cid_var153_chA_0 -> ending { },
    starting -> started { sync async_func79[pid]?; assign is_sync = false, initialize(); },
    starting -> started { sync sync_func79[pid]?; assign is_sync = true, initialize(); },
    ending -> ended { guard is_sync == false; },
    ending -> ended { guard is_sync == true; sync sync_func79[pid]!; };
}

Channel0 = Channel(0);
Channel1 = Channel(1);
Channel2 = Channel(2);
Channel3 = Channel(3);
Channel4 = Channel(4);
Channel5 = Channel(5);
Channel6 = Channel(6);
Channel7 = Channel(7);
Channel8 = Channel(8);
Channel9 = Channel(9);
func790 = func79(0);
func800 = func80(0);
system Channel0, Channel1, Channel2, Channel3, Channel4, Channel5, Channel6, Channel7, Channel8, Channel9, func790, func800, main;


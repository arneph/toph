// Place global declarations here.
int fid_var27_main = 15;

int func16_count = 0;
int cap_cid_var30_chA[1];
chan async_func16[1];
chan sync_func16[1];
int make_func16() {
    int pid = func16_count;
    func16_count++;
    return pid;
}

int func17_count = 0;
int cap_cid_var31_chA[1];
chan async_func17[1];
chan sync_func17[1];
int make_func17() {
    int pid = func17_count;
    func17_count++;
    return pid;
}

int chan_count = 0;
int chan_counter[10];
int chan_buffer[10];
chan sender_alpha[10];
chan sender_omega[10];
chan receiver_alpha[10];
chan receiver_omega[10];
chan close[10];

int make_chan(int buffer) {
    int cid = chan_count;
    chan_count++;
    chan_counter[cid] = 0;
    chan_buffer[cid] = buffer;
    return cid;
}


process Channel(int[0, 9] i) {
// Place local declarations here.

state
    idle,
    confirming_a,
    confirming_closing,
    closing,
    closed,
    confirming_closed,
    new_sender,
    new_receiver,
    confirming_b,
    bad;
commit
    confirming_closing,
    closing,
    confirming_closed,
    confirming_a,
    confirming_b,
    new_sender,
    new_receiver;
init
    idle;
trans
    idle -> new_sender { sync sender_alpha[i]?; assign chan_counter[i]++; },
    new_sender -> idle { guard chan_counter[i] > chan_buffer[i]; },
    new_sender -> confirming_a { guard chan_counter[i] <= chan_buffer[i]; sync sender_omega[i]!; },
    confirming_a -> idle { guard chan_counter[i] > 0; },
    confirming_a -> idle { guard chan_counter[i] <= 0; sync receiver_omega[i]!; },
    idle -> new_receiver { sync receiver_alpha[i]?; assign chan_counter[i]--; },
    new_receiver -> idle { guard chan_counter[i] < 0; },
    new_receiver -> confirming_b { guard chan_counter[i] >= 0; sync receiver_omega[i]!; },
    confirming_b -> idle { guard chan_counter[i] < chan_buffer[i]; },
    confirming_b -> idle { guard chan_counter[i] >= chan_buffer[i]; sync sender_omega[i]!; },
    idle -> confirming_closing { guard chan_counter[i] < 0; sync close[i]?; },
    confirming_closing -> confirming_closing { guard chan_counter[i] < 0; sync receiver_omega[i]!; assign chan_counter[i]++; },
    confirming_closing -> closing { guard chan_counter[i] == 0; },
    idle -> closing { guard 0 <= chan_counter[i] && chan_counter[i] <= chan_buffer[i]; sync close[i]?; },
    idle -> bad { guard chan_counter[i] > chan_buffer[i]; sync close[i]?; },
    closing -> closed { assign chan_counter[i] = 1,
chan_buffer[i] = -1; },
    closed -> confirming_closed { sync receiver_alpha[i]?; },
    confirming_closed -> closed { sync receiver_omega[i]!; },
    closed -> bad { sync sender_alpha[i]?; },
    closed -> bad { sync close[i]?; };
}

process main() {
// Place local declarations here.
bool is_sync;
int p = -1;
int cid_var28_chA = -1;
int cid_var29_chB = -1;

state
    made_cid_var28_chA_0,
    made_cid_var29_chB_0,
    closed_cid_var29_chB_0,
    created_func16_0,
    started_func17_0,
    starting,
    ending,
    ended,
    started_func16_0,
    created_func17_0,
    started;
init
    starting;
trans
    started -> made_cid_var28_chA_0 { assign cid_var28_chA = make_chan(0); },
    made_cid_var28_chA_0 -> made_cid_var29_chB_0 { assign cid_var29_chB = make_chan(0); },
    made_cid_var29_chB_0 -> closed_cid_var29_chB_0 { sync close[cid_var29_chB]!; },
    closed_cid_var29_chB_0 -> created_func16_0 { assign p = make_func16(), cap_cid_var30_chA[p] = cid_var28_chA; },
    created_func16_0 -> started_func16_0 { sync async_func16[p]!; },
    started_func16_0 -> created_func17_0 { assign p = make_func17(), cap_cid_var31_chA[p] = cid_var28_chA; },
    created_func17_0 -> started_func17_0 { sync async_func17[p]!; },
    started_func17_0 -> ending { },
    starting -> started { },
    ending -> ended { };
}

process func16(int[0, 0] pid) {
// Place local declarations here.
bool is_sync;
int p = -1;
int cid_var30_chA = -1;
void initialize() {
    cid_var30_chA = cap_cid_var30_chA[pid];
}

state
    ended,
    exit_loop_cond0,
    enter_loop_body0,
    exit_loop_body0,
    exit_loop0,
    started,
    ending,
    enter_loop_cond0,
    starting;
init
    starting;
trans
    enter_loop_cond0 -> exit_loop_cond0 { },
    enter_loop_body0 -> exit_loop_body0 { },
    started -> enter_loop_cond0 { },
    exit_loop_cond0 -> enter_loop_body0 { },
    exit_loop_cond0 -> exit_loop0 { },
    exit_loop_body0 -> enter_loop_cond0 { },
    exit_loop0 -> ending { },
    starting -> started { sync async_func16[pid]?; assign is_sync = false, initialize(); },
    starting -> started { sync sync_func16[pid]?; assign is_sync = true, initialize(); },
    ending -> ended { guard is_sync == false; },
    ending -> ended { guard is_sync == true; sync sync_func16[pid]!; };
}

process func17(int[0, 0] pid) {
// Place local declarations here.
bool is_sync;
int p = -1;
int cid_var31_chA = -1;
void initialize() {
    cid_var31_chA = cap_cid_var31_chA[pid];
}

state
    range_cond0,
    range_receiving_cid_var31_chA_0,
    enter_loop_body0,
    exit_loop0,
    starting,
    started,
    ending,
    ended,
    exit_loop_body0;
init
    starting;
trans
    enter_loop_body0 -> exit_loop_body0 { },
    started -> range_cond0 { },
    range_cond0 -> range_receiving_cid_var31_chA_0 { guard chan_buffer[cid_var31_chA] >= 0; sync receiver_alpha[cid_var31_chA]!; },
    range_receiving_cid_var31_chA_0 -> enter_loop_body0 { sync receiver_omega[cid_var31_chA]?; },
    range_cond0 -> exit_loop0 { guard chan_buffer[cid_var31_chA] < 0; },
    exit_loop_body0 -> range_cond0 { },
    exit_loop0 -> ending { },
    starting -> started { sync async_func17[pid]?; assign is_sync = false, initialize(); },
    starting -> started { sync sync_func17[pid]?; assign is_sync = true, initialize(); },
    ending -> ended { guard is_sync == false; },
    ending -> ended { guard is_sync == true; sync sync_func17[pid]!; };
}

Channel0 = Channel(0);
Channel1 = Channel(1);
Channel2 = Channel(2);
Channel3 = Channel(3);
Channel4 = Channel(4);
Channel5 = Channel(5);
Channel6 = Channel(6);
Channel7 = Channel(7);
Channel8 = Channel(8);
Channel9 = Channel(9);
func160 = func16(0);
func170 = func17(0);
system Channel0, Channel1, Channel2, Channel3, Channel4, Channel5, Channel6, Channel7, Channel8, Channel9, func160, func170, main;

